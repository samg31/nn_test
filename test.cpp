#include <iostream>
#include <fstream>
#include "neural_network.hpp"
#include <vector>

int main()
{

  std::vector<std::vector<int>> input;
  
  std::ifstream reader("");
  
  while(!reader.eof())
  {
    for(int i = 0; i < 9; ++i)
    {
    int current;
       getline(reader, current);
       
       


NeuralNetwork brain()

return 0;
}
